import { DiscloudConfig } from "../DiscloudConfig";

{
  const dConfig = new DiscloudConfig(<string><unknown>undefined);

  dConfig.comments;
  dConfig.data;
  dConfig.exists;
  dConfig.existsMain;
  dConfig.fileExt;
  dConfig.get("MAIN");
  dConfig.missingProps;
  dConfig.path;
}
